const render = () => {
    const template = (
        <div>
            <h1>React Components</h1>

        </div>
    );

    ReactDOM.render(template, document.getElementById('app'));
}

render();